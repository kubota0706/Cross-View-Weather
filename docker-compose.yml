services:
  fastapi:
    build: .
    container_name: cross-view-weather
    tty: true
    stdin_open: true
    volumes:
      - ./fast_api:/fast_api
    ports:
    - "127.0.0.1:8080:8080"
    command: /bin/bash
    environment:
      - PYTHONUNBUFFERED=1